import { input } from './input';
import { funA } from './x';

describe('3', () => {
  test('A', () => {
    expect(funA(input)).toBe(1);
  });

  test('B', () => {
    expect(funA(input)).toBe(1);
  });
});
